# ── Core framework ──────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn==0.34.2
pydantic==2.11.4
python-dotenv==1.1.0
python-multipart==0.0.20

# ── Databases ────────────────────────────────────────────────────────────────
neo4j==5.28.1
qdrant-client==1.13.3
grpcio==1.71.0
grpcio-tools==1.71.0

# ── LLM / OpenAI-compatible client ──────────────────────────────────────────
openai==1.82.0

# ── PyTorch pinned — satisfies all downstream torch>=2.0.0 constraints ───────
# Installed in Dockerfile Step 1 via the CUDA wheel index.
# Pinning here prevents pip from re-downloading a newer torch from PyPI.
torch==2.5.1

# ── HuggingFace stack (pinned, compatible with transformers 4.51.3) ───────────
# transformers 4.51.3 requires:  huggingface-hub>=0.30,<1.0  AND  tokenizers>=0.21,<0.22
huggingface-hub==0.30.2
transformers==4.51.3
tokenizers==0.21.1
safetensors==0.5.3
accelerate==1.6.0

# ── Embeddings / Reranker ────────────────────────────────────────────────────
FlagEmbedding==1.3.5
numpy==1.26.4

# ── NER ─────────────────────────────────────────────────────────────────────
gliner==0.2.21

# ── Document parsing ─────────────────────────────────────────────────────────
docling==2.38.0